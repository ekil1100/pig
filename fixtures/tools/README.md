# Tool Fixtures

Offline fixtures for M3 built-in coding tools.

Tests must copy or create temp workspaces before mutation. Do not store secrets, private data, or machine-specific absolute paths here.
