# M0 Fixtures

These fixtures are small, offline-only samples used by Pig v1.0 M0 tests.

Rules:

- Do not include secrets, API keys, auth tokens, or real user sessions.
- Do not copy large source trees from reference projects.
- Prefer compact metadata and behavior samples that are stable in tests.
